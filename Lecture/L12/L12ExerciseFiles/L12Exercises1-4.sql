/* COMP 3311: L12Exercises1-4.sql */

--------------------------------------------------------------------------------
/* Exercise 1:  Construct a JSON object for each tuple in the Sailor relation 
                with fields all the Sailor relation attributes; a field whose 
                value is null should be absent.
				Order the quey result objects by sailor id ascending. */
-- <<< Construct the SQL query below this line as a single SQL statement >>>



--------------------------------------------------------------------------------
/* Exercise 2:  Construct a JSON array for each tuple in the Sailor relation 
                with elements all the values of the Sailor tuple ordered by 
                rating descending. An array element whose value is null should 
                be included. */
-- <<< Construct the SQL query below this line as a single SQL statement >>>



--------------------------------------------------------------------------------
/* Exercise 3:  Construct a JSON object for each tuple in the Sailor relation 
                with fields sailor id, name and reservations which is an object 
                containing, as its fields, the field boat id and its value for 
                each boat that the sailor has reserved; if a sailor has no 
                reservations, then the reservations field should be absent. */
-- <<< Construct the SQL query below this line as a single SQL statement >>>



--------------------------------------------------------------------------------
/* Exercise 4:  Construct a JSON object for each tuple in the Sailor relation 
                with fields sailor id, name and "reserved boats" which is an 
                array field whose elements are the boat id values, in ascending 
                order, of the boats that the sailor has reserved. If a sailor 
                has not reserved any boat, then the "reserved boats" field 
                should be absent.
                Order the query result objects by sailor id ascending. */
-- <<< Construct the SQL query below this line as a single SQL statement >>>



--------------------------------------------------------------------------------
